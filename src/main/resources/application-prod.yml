spring:
  datasource:
    url: ${SPRING_DATABASE_URL:jdbc:postgresql://postgres:5432/iwvgdevopsdb}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false