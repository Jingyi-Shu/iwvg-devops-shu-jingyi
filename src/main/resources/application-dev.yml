server:
  port: 8080

logging:
  level:
    root: WARN
    es.upm.api: DEBUG

app:
  hosting: localhost

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/iwvgdevopsdb
    username: postgres
    password: postgres
